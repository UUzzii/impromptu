# 端口
server:
  port: 9030

spring:
  application:
    # 应用名称
    name: impromptu-admin
  # 数据库配置
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://139.199.230.186:3306/impromptu?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&autoReconnect=true&keepAlive=true
    username: root
    password: shipeng@98
    druid:
      # 初始连接数
      initial-size: 5
      # 最小连接池数量
      min-idle: 5
      # 最大连接池数量
      max-active: 10
      # 获取连接等待超时时间
      max-wait: 60000
  # redis配置
  redis:
    host: 139.199.230.186
    port: 6379
    password: redis980520
    timeout: 10000
  # 全局日期格式化
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss

mybatis-plus:
  configuration:
    # 驼峰命名
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 主键类型
      id-type: auto
      # 表前缀
      table-prefix: mb_
  # 别名包扫描路径
  type-aliases-package: com.impromptu.admin.entity