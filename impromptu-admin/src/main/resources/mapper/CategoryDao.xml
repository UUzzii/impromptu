<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.impromptu.admin.dao.CategoryDao">

    <resultMap type="com.impromptu.admin.entity.Category" id="CategoryMap">
        <result property="id" column="id" />
        <result property="parentId" column="parent_id" />
        <result property="name" column="name" />
        <result property="image" column="image" />
        <result property="remark" column="remark" />
        <result property="sort" column="sort" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO mb_category(parent_id, name, image, remark, sort, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.parentId}, #{item.name}, #{item.image}, #{item.remark}, #{item.sort}, #{item.createTime}, #{item.updateTime})
        </foreach>
        ON DUPLICATE KEY UPDATE
        parent_id = VALUES(parent_id),
        name = VALUES(name),
        image = VALUES(image),
        remark = VALUES(remark),
        sort = VALUES(sort),
        create_time = VALUES(create_time),
        update_time = VALUES(update_time)
    </insert>

</mapper>

