<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.impromptu.admin.dao.AdminUserDao">

    <resultMap type="com.impromptu.admin.entity.AdminUser" id="AdminUserMap">
        <result property="id" column="id" />
        <result property="account" column="account" />
        <result property="password" column="password" />
        <result property="name" column="name" />
        <result property="phone" column="phone" />
        <result property="sex" column="sex" />
        <result property="avatar" column="avatar" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO mb_admin_user(account, password, name, phone, sex, avatar, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.account}, #{item.password}, #{item.name}, #{item.phone}, #{item.sex}, #{item.avatar}, #{item.status}, #{item.createTime}, #{item.updateTime})
        </foreach>
        ON DUPLICATE KEY UPDATE
        account = VALUES(account),
        password = VALUES(password),
        name = VALUES(name),
        phone = VALUES(phone),
        sex = VALUES(sex),
        avatar = VALUES(avatar),
        status = VALUES(status),
        create_time = VALUES(create_time),
        update_time = VALUES(update_time)
    </insert>

</mapper>

